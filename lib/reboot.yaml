- hosts: "{{ myhosts.split('-')[0] }}-proxy-02"
  tasks:
    - import_tasks: tasks/sensu_silence.yaml
      vars:
        mute_time: "1800"
- hosts: "{{ myhosts }}"
  tasks:
    - name: Rebooting...
      ansible.builtin.reboot:
        reboot_timeout: 1800
